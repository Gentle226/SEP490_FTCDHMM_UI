{
  "name": "base-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "web": "pnpm --filter next-app",
    "mobile": "pnpm --filter mobile-app",
    "dev": "pnpm --filter next-app dev",
    "build": "pnpm --filter next-app build",
    "start": "pnpm --filter next-app start",
    "lint": "pnpm --filter next-app lint",
    "storybook": "pnpm --filter next-app dev:storybook",
    "build-storybook": "pnpm --filter next-app build-storybook"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "3.5.3",
    "typescript": "^5"
  },
  "dependencies": {
    "cookies-next": "^6.1.0"
  }
}
